syntax = "proto3";

service CAAChecker {
        rpc ValidForIssuance(Check) returns (Result) {}
}

message Check {
        string name = 1;
        string issuerDomain = 2;
}

message Result {
        bool present = 1;
        bool valid = 2;
}