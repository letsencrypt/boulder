syntax = "proto3";

service CAAChecker {
        rpc ValidForIssuance(Check) returns (Valid) {}
}

message Check {
        string name = 1;
        string issuerDomain = 2;
}

message Valid {
        bool present = 1;
        bool valid = 2;
}