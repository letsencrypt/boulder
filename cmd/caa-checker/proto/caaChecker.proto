syntax = "proto2";

service CAAChecker {
        rpc ValidForIssuance(Check) returns (Result) {}
}

message Check {
        required string name = 1;
        required string issuerDomain = 2;
}

message Result {
        required bool present = 1;
        required bool valid = 2;
}
